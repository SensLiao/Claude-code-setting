# Project: {project_name}

> Generated by `claude-env-bootstrap` on {date}
> To re-sync with global ~/.claude/: invoke `claude-env-bootstrap --update`
> Manifest: `.claude/manifest.json`
> Platform/model facts (model ids, effort levels, Dynamic Workflows, fast mode, prompt-cache): see `~/.claude/docs/native-capabilities.md` — single source of truth. Do NOT copy platform facts inline here (prevents per-project drift).

---

## Bootstrap Manifest

**Combo composed for**: {one-line user description from Q1}
**Delivery form**: {Q2 answer}
**Tech stack(detected)**: {auto-detected}

### Skills installed({N})
{list — bullet format with one-line reason per skill}
- ★ `{primary-methodology-skill}` — {主方法论:为什么选}
- `{skill 2}` — {为什么装}
- `{skill 3}` — {为什么装}
- ...

### Rules installed
- `.claude/rules/common/*` — {复制了 common rules}
- `.claude/rules/{lang}/*` — {复制了语言特定 rules}
- (或) referenced from `~/.claude/rules/`(未复制,依赖全局)

### Workflow defaults
- 默认走 GSD: `/gsd-new-project`, `/gsd-spec-phase`, `/gsd-plan-phase`, `/gsd-execute-phase`, `/gsd-verify-work`
- {根据 combo 加的额外 workflow,例:"UI 设计走 sens-frontend-design 3-stage 流程"}

---

## Project

> 用户填写。一句话描述项目目标 + 客户 + 交付物。
> 例:"为澳大利亚少儿教育机构开发提案级 demo,含 WordPress 公开官网 + 自建内部教务系统。"

{TODO: 用户填写或用 /init 跑出来}

---

## Constraints

> 业务 / 技术 / 合规 / 时间 / 预算约束。

{TODO: 用户填写}

---

## Tech Stack

> 自动检测结果(claude-env-bootstrap 写入)。

{auto-detected from package.json / pubspec.yaml / etc.}

例:
- Frontend: Next.js 14 + TypeScript + Tailwind + shadcn/ui
- Backend: Supabase(Auth / Postgres / Storage / RLS)
- Deploy: Vercel
- Database: Postgres(Supabase managed)

---

## Conventions

> 项目编码约定。如有项目特定的命名 / 文件组织 / 状态管理偏好,在此声明。

{TODO: 用户填写,或在做过几个 phase 后从代码反推}

---

## Architecture

> 项目架构。如已用 `gsd-map-codebase` 跑过,可引用 `.planning/codebase/` 的产物。

{TODO: 用户填写,或先跳过用 `/gsd-map-codebase` 自动生成}

---

## Skill Routing(项目特定)

> 哪些任务走哪个 skill。覆盖全局 CLAUDE.md 的路由规则。

{根据 combo 自动生成的 routing,例如:}

| 任务类型 | 推荐 skill |
|---|---|
| 新需求拆解 | `gsd-spec-phase` → `gsd-plan-phase` |
| 设计探索(UI 项目) | `{主方法论 skill}` |
| 视觉风格锁定 | `{选定的 L3 风格}` |
| 上线前 audit | `gsd-ui-review` / `ux-principles`(MODE B) |
| 部署 | `{平台 skill,例 vercel:deploy}` |

---

## Hard Rules(继承全局 + 项目补充)

继承全局 `~/.claude/CLAUDE.md` 的硬规则(不可变性 / 不硬编码密钥 / 测试覆盖 / 文件 <800 行 / 先验证再声称完成等),外加本项目的:

{TODO: 项目特定铁律,例:"全站文案中英双语 / 货币只用 AUD / 不接在线支付 / 不存储未脱敏 PII"}

---

## Workflow Enforcement

继承全局的 GSD 启动协议:
- 任何非 trivial 工作 → 先 `gsd-pipeline-orchestrator`
- 简单任务可跳 GSD,中等任务用 `gsd-quick`,复杂任务走完整流程

{根据装的 skills 添加额外约束,例:}
- {若装了 sens-frontend-design} 设计任务 → 走 3-stage 流程(Reference → Anchors → Prototype)
- {若装了 luxury-editorial-site-builder} 单页 brand landing → 走该 skill

---

## Developer Profile

> Profile not yet configured. Run `/gsd-profile-user` to generate.
> This section is managed by `generate-claude-profile` — do not edit manually.

---

## Notes / TODO

{TODO: 用户随手记}
